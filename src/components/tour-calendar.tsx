'use client';
import {useState} from 'react';
import {ChevronLeft,ChevronRight} from 'lucide-react';
import {events} from '@/lib/catalog';
export function TourCalendar({onSelect}:{onSelect:(id:string)=>void}){const [offset,setOffset]=useState(0);const month=new Date(Date.UTC(2026,9+offset,1));const y=month.getUTCFullYear(),m=month.getUTCMonth(),days=new Date(Date.UTC(y,m+1,0)).getUTCDate(),first=(month.getUTCDay()+6)%7;return <div className="tour-calendar"><div className="label-row"><h3>{month.toLocaleDateString('en-IN',{month:'long',year:'numeric',timeZone:'UTC'})}</h3><div><button className="icon-button" aria-label="Previous calendar month" disabled={offset<=0} onClick={()=>setOffset(offset-1)}><ChevronLeft size={18}/></button><button className="icon-button" aria-label="Next calendar month" disabled={offset>=2} onClick={()=>setOffset(offset+1)}><ChevronRight size={18}/></button></div></div><div className="calendar-grid">{['M','T','W','T','F','S','S'].map((d,i)=><span className="calendar-weekday" key={'w'+i}>{d}</span>)}{Array.from({length:first},(_,i)=><span key={'blank'+i}/>)}{Array.from({length:days},(_,i)=>{const day=i+1;const date=`${y}-${String(m+1).padStart(2,'0')}-${String(day).padStart(2,'0')}`;const event=events.find(e=>e.date===date);return event?<button key={day} className="calendar-event" aria-label={`${event.city}, ${event.name}, ${date}`} onClick={()=>onSelect(event.id)}>{day}<small>{event.city}</small></button>:<span className="calendar-day" key={day}>{day}</span>;})}</div><p className="sample-note">Highlighted dates are sample pop-ups. Select one to explore its rework slots.</p></div>;}
